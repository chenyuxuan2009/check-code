#include <bits/stdc++.h>
using namespace std;
int main()
{
    srand(time(0));
    cout << 1LL * rand() * rand() * rand() * rand() << " " << 1LL * rand() * rand() * rand() * rand() << endl;
    return 0;
}
